#version 330 core
in vec2 UV;

out vec3 color;

uniform sampler2D renderTexture;
uniform float time;

void main()
{
	color = texture(renderTexture, UV + 0.005 * vec2(sin(time + 640 * UV.x), cos(time + 480*UV.y))).xyz;
}